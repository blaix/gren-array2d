{
  "private": true,
  "scripts": {
    "test": "cd tests && gren make src/Main.gren && node app"
  },
  "dependencies": {
    "gren-lang": "^0.5.2"
  },
  "devDependencies": {
    "gren-packages": "^5.0.0"
  }
}
